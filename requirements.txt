-e .

# Query-time structured analysis for chat-with-data.
duckdb>=1.0,<2
# Retained for the standalone local Ollama starter and later narrative milestone.
ollama==0.6.2
# Read-only validation and normalization of approved .xlsx inputs.
openpyxl==3.1.5
# Deterministic evidence charts rendered with the non-interactive Agg backend.
matplotlib==3.11.1
# Print-ready report export and PDF verification.
reportlab==4.5.1
